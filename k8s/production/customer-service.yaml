apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: production
spec:
  selector:
    app: customer
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
