apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: staging
spec:
  selector:
    app: product
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
